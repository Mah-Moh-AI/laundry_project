// not used --> The sequelize log format is as per config/database.js file

// const npmPackages = require("../utils/npmPackages");
// const sequelize = npmPackages.Sequelize;

// // Create a Sequelize logger instance for SQL query logging
// const sequelizeLogger = () => {
//   return new Sequelize({
//     logging: (sql, timing) => {
//       databaseLogger.info(`SQL Query: ${sql}`);
//       databaseLogger.info(`Execution Time: ${timing}ms`);
//     },
//   });
// };

// module.exports = sequelizeLogger;
